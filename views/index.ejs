<!DOCTYPE html>
<html>
<head>
    <title>Furniture Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    
    <nav class="navbar navbar-dark bg-dark mb-4 sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">🏠 Furniture Store</a>
            <a class="btn btn-outline-light" href="/admin">Login Admin</a>
        </div>
    </nav>

    <div class="container">
        <div class="p-4 mb-4 bg-secondary text-white rounded text-center">
            <h1>Selamat Datang di Toko Furniture</h1>
            <p>Temukan furniture terbaik untuk rumah Anda</p>
        </div>

        <h3 class="mb-3 border-bottom pb-2">Katalog Produk</h3>
        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm border-0">
                        
                        <img src="/images/<%= product.image %>" class="card-img-top" alt="<%= product.name %>" style="height: 250px; object-fit: cover;">
                        
                        <div class="card-body">
                            <h5 class="card-title fw-bold"><%= product.name %></h5>
                            
                            <h5 class="text-primary fw-bold"><%= formatRupiah(product.price) %></h5>
                            
                            <hr>
                            <p class="card-text text-muted small">
                                Stock Tersedia: <strong class="text-dark"><%= product.quantity %> Unit</strong>
                            </p>

                            <% if(product.quantity > 0) { %>
                                <form action="/buy" method="POST">
                                    <input type="hidden" name="source" value="user">
                                    <input type="hidden" name="product_id" value="<%= product.id %>">
                                    
                                    <div class="input-group mt-3">
                                        <input type="text" name="customer_name" class="form-control" placeholder="Nama Kamu..." required>
                                        <button class="btn btn-success" type="submit">🛒 Beli</button>
                                    </div>
                                </form>
                            <% } else { %>
                                <div class="d-grid mt-3">
                                    <button class="btn btn-secondary disabled">❌ Stock Habis</button>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer class="text-center text-muted py-4">
        &copy; 2025 Akmal Zaidan Ravian
    </footer>
</body>
</html>